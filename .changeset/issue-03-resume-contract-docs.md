---
"@ai-hero/sandcastle": patch
---

Clarify the `resumeSession` contract in the docs by explicitly separating Claude Code's host-captured resume flow from provider-native resume providers such as Kiro, including when Sandcastle reports captured session metadata like `sessionFilePath` and `usage`.
